```bash
git config --global user.name "Juan Pablo Aparo"
```
```bash
git config --global user.mail "optimidesarrollo@gmail.com"
```
# 
**IMPORTANTE**: GIT no versiona carpetas vacias.

git statusgit 
**IMPORTANTE**: GIT no versiona carpetas vacias.

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/JuanPabloAparo/git.git
git push -u origin main


git remote add origin https://github.com/JuanPabloAparo/git.git
git branch -M main
git push -u origin main